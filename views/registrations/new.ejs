<%- include('../partials/header') %>

    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card shadow-sm">
                <div class="card-header bg-primary text-white">
                    <h5 class="mb-0"><i class="bi bi-person-plus me-2"></i>New Registration</h5>
                </div>
                <div class="card-body p-4">

                    <% if (errors && errors.length> 0) { %>
                        <div class="alert alert-danger">
                            <ul class="mb-0">
                                <% errors.forEach(e=> { %><li>
                                        <%= e %>
                                    </li>
                                    <% }); %>
                            </ul>
                        </div>
                        <% } %>

                            <form action="/registrations" method="POST">

                                <!-- Participant dropdown populated from User collection -->
                                <div class="mb-3">
                                    <label for="participant" class="form-label fw-semibold">
                                        Participant <span class="text-danger">*</span>
                                    </label>
                                    <select class="form-select" id="participant" name="participant" required>
                                        <option value="">-- Select Participant --</option>
                                        <% users.forEach(u=> { %>
                                            <option value="<%= u._id %>">
                                                <%= u.username %>
                                            </option>
                                            <% }); %>
                                    </select>
                                </div>

                                <!-- Event dropdown populated from Event collection -->
                                <div class="mb-3">
                                    <label for="event" class="form-label fw-semibold">
                                        Event <span class="text-danger">*</span>
                                    </label>
                                    <select class="form-select" id="event" name="event" required>
                                        <option value="">-- Select Event --</option>
                                        <% events.forEach(ev=> { %>
                                            <option value="<%= ev._id %>">
                                                <%= ev.eventName %> (<%= new
                                                        Date(ev.eventDate).toLocaleDateString('en-US', {
                                                        dateStyle: 'medium' }) %>)
                                            </option>
                                            <% }); %>
                                    </select>
                                </div>

                                <div class="mb-4">
                                    <label for="status" class="form-label fw-semibold">Status</label>
                                    <select class="form-select" id="status" name="status">
                                        <option value="Pending" selected>Pending</option>
                                        <option value="Approved">Approved</option>
                                        <!-- Rejected is only set when editing an existing registration -->
                                    </select>
                                </div>

                                <div class="d-flex gap-2">
                                    <button type="submit" class="btn btn-primary">
                                        <i class="bi bi-save me-1"></i>Save Registration
                                    </button>
                                    <a href="/registrations" class="btn btn-outline-secondary">Cancel</a>
                                </div>

                            </form>
                </div>
            </div>
        </div>
    </div>

    <%- include('../partials/footer') %>